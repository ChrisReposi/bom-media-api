# Backup Script Templates

These files are examples only. They are not installed as cron jobs and do not prove backups are running.

Use them as starting points for an operator-managed backup workflow:

- `mysql-dump.example.sh` creates a logical MySQL/MariaDB dump from environment variables.
- `restore-checklist.md` records manual restore evidence.

Never commit credentials or generated backup artifacts.

